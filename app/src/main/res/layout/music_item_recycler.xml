<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#EFEFEF"
    android:orientation="horizontal"
    android:layout_marginBottom="10dp"
    android:padding="@dimen/songs_padding">

    <ImageView
        android:id="@+id/music_icon"
        android:layout_width="@dimen/weight_music_logo"
        android:layout_height="@dimen/height_music_logo"
        android:src="@drawable/logo" />

    <TextView
        android:id="@+id/music_title_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:ellipsize="end"
        android:layout_marginStart="@dimen/margin_music_title"
        android:maxLines="1"
        android:textColor="@color/black"
        tools:text="Music" />
</LinearLayout>